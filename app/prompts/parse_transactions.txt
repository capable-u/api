You receive raw text extracted from a German bank statement PDF.

Your task:
1. Extract only actual bank transactions.
2. Ignore headers, balances, page footers, IBAN metadata, legal text, summaries.
3. Return transactions as strict JSON only.
4. If category is unclear, use the category_id for "Other" from the provided ID list.
5. Use:
   - direction = "income" for positive incoming money
   - direction = "expense" for outgoing money
6. Keep raw_description as close as possible to the source text.
7. Generate normalized_description from context: short, clear, no filler words.
   - Keep only key facts: operation type, merchant/counterparty, and important reference/details if present.
   - Remove boilerplate text, long legal/service fragments, repeated tokens, and noise.
   - Do not lose critical transaction meaning; if details are important for identification, keep them.
   - Prefer one compact phrase, typically 3-10 words.
8. booking_date must be ISO format YYYY-MM-DD.
9. currency should default to EUR unless another currency is explicitly visible.
10. confidence should reflect confidence in the category, not whether the row is a transaction.
11. category_id must be one of the IDs from the "ALLOWED CATEGORIES WITH IDS" list.
12. Never invent category IDs.

Important:
- Return only real transactions.
- Do not invent missing data.
- Do not merge two transactions into one.
- If a field is unknown, use null where allowed.
- Each transaction JSON object must include normalized_description (or null if truly unavailable).
